apiVersion: v1
kind: Service
metadata:
  name: devops-demo-svc
  namespace: production
  labels:
    app: devops-demo
    environment: production
spec:
  type: ClusterIP
  selector:
    app: devops-demo
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
